export interface ITechnologyType {
    id: number
    name: string
    icon: string
    description: string
    category: string
    level: string
    rating: number
    badge: string
    color:string
}